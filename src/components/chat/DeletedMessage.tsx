import React from "react";

export default function DeletedMessage() {
  return (
    <div style={{ fontStyle: "italic", color: "#aaa", fontSize: 14, padding: 10, textAlign: "center" }}>
      🚫 Questo messaggio è stato eliminato.
    </div>
  );
}
